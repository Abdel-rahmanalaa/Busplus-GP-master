import 'package:flutter/material.dart';

class PassengerShareLocationScreen extends StatelessWidget {
  const PassengerShareLocationScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Share location'),
      ),
    );
  }
}